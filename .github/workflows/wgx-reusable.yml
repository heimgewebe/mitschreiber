name: Reusable WGX
on:
  workflow_call:

jobs:
  wgx:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name of: Run smoke task
        run: wgx smoke

      - name of: Run guard task
        run: wgx guard
